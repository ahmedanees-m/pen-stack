# data is fetched, not committed (manifests + small curated tables only)
data/raw/
data/interim/
data/features/
data/models/
data/out/
data/external/
*.bw
*.bam
*.fa
*.fasta
*.gz
# large parquet artifacts (curated small tables are force-added explicitly)
features/
models/
out/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
# the setuptools dist build dir at repo ROOT only (NOT pen_stack/build/ the v5.11 package)
/build/

# v6.2 web frontend, build artifacts + deps + local env (never commit)
web/node_modules/
web/dist/
web/.env

# secrets, NEVER commit
.pen/
secrets.env
*.pat
Keys.txt
GitHub_Access_Token.txt
configs/nvidia_api_key.txt

# unpublished manuscripts, kept local only, never public
manuscripts/

# os/editor
.DS_Store
.ipynb_checkpoints/
.vscode/

# LLM provider secrets (never commit)
configs/nvidia_api_key.txt
configs/*_api_key.txt
.env
configs/alphagenome_api_key.txt

# v6.6: bring-your-own-license restricted data (COSMIC/OncoKB), never commit
licensed_data/
